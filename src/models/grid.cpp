#include "models/grid.hpp"

Grid::Grid(std::size_t height, std::size_t width) : width(width),
                                                    height(height)
{
    /* vecor initilaisaiton*/
}

Grid::~Grid()
{
}